{
  "name": "chatappproject",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {
    "concurrently": "^9.1.2",
    "gh-pages": "^6.3.0"
  },
 
  "scripts": {
    "heroku-postbuild": "cd client && npm install && npm run build",
    "start": "cd server && node index.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7"
  }
}
